#!/bin/bash

selection=$(tmux list-sessions | sed -E 's/:.*$//' | grep -v \"^$(tmux display-message -p '#S')\$\" | fzf --reverse)

if [[ -z $selected ]]; then
    exit 0
fi

tmux switch-client -t $selection
